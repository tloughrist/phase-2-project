import React from "react";

function AllFormations({ searchValue, currentUser, userData }) {

    return (
        <div id="formation-array-container">
            <h1>All Formations</h1>
        </div>
    );
};

export default AllFormations;